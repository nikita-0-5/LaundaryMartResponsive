**Laundry Services - Hero Section - Responsive design**
 Topics Covered:
    >>Responsive Navigation Bar
    >>Hero Section with Call-to-Action
    >>CSS Variables for easy theme customization
    >>Smooth Hover Effects
    >>Hamburger Menu for Small Screens
    >>Mobile Responsive Layout
    >>Animation keyframes
    

File Structure:
    >>Nikita_Task10/ main
        >>index.html
        >>style.css
        >>README.md
        >>logo.png
        >>image.png

Steps:
    >>Created HTML and CSS files
    >>Linked the style sheet to HTML
    >>Created HTML structure
        >>Body
            >>Header
                >>Logo
                >>Navigation Bar
                >>Username Button
                >>Hamburger for mobile screen size with menuu list
            >>Hero Section
                >>Left Content
                >>Right Image

    >>Gave style to the page using CSS
        >>Removed extra margin and padding from bosy and set box-sizing as border-box
        >>Created CSS variables to customise theme
        >>Styled Header using display as flex, gave hover effects to nav bar and username button
        >>Added menu hamburger and menu list for mobile screen and hidden them for default
        >>syled Lest side content using flex and flex basis
        >>Styled right side image using flex and flex basis

        >>used @media to give responsiveness to the page- for tab: lesser the size, for mobile: removed nav bar instead used hamburger and menu list.

        >>Gave transform effect to the button in left, when hovered a bit tilted (rotate) and size increase (scale).
        >>Gave animation to the image in right to make a orbit and some otehr animation too.

How to Run:
    >>Clone or download this folder.
    >>Open index.html directly in any web browser.
